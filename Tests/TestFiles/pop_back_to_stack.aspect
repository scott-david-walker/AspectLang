// Tests whether we correctly pop the stack values. the "+ 1" is only used when wee go back up the stack
fn pop_back(x) {
    if(x > 35) {
        return x;
    }
    return pop_back(x * 2) + 1;
}

return pop_back(2);